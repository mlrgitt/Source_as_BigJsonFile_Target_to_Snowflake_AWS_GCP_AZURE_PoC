# Source_as_BigJsonFile_Target_to_Snowflake_AWS_GCP_AZURE_PoC
Aim to process 20GB nested JSON using Spark, flatten it, and apply CDC using Delta MERGE. This avoids full reloads, ensures scalability, and supports schema evolution.
